<?xml version="1.0" encoding="utf-8"?>
<manifest package="com.sumauto.habitat"
          xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:name=".HabitatApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activity.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name="com.sumauto.activity.PhotoAlbumActivity"/>
        <activity android:name=".activity.search.SearchActivity">
        </activity>
        <activity
            android:name=".activity.search.AddressBookActivity"
            android:label="@string/address_book">
        </activity>
        <activity android:name=".activity.trend.TrendDetailActivity">
        </activity>
        <activity
            android:name=".activity.publish.PublishActivity"
            android:windowSoftInputMode="stateHidden">
        </activity>
        <activity android:name=".activity.publish.NoticeFriendListActivity"
            android:label="@string/notice_who_see">
        </activity>
    </application>

</manifest>
